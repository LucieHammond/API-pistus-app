{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création de nouvelle</h1>
    {% form_theme form 'form_table_layout.html.twig' %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer la nouvelle" />
    {{ form_end(form) }}

            <a href="{{ path('admin_new_index') }}">Retour vers la liste</a>
{% endblock %}
