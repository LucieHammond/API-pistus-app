{% extends 'base.html.twig' %}

{% block body %}
    <h1>Contest</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contest.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contest.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('contest_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
